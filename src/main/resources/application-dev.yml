spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/arca
    username: arca
    password: "012123"
    driver-class-name: org.postgresql.Driver

  security:
    user:
      name: Jhanell
      password: 012123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
  address: 0.0.0.0
  port: 8080

